
# This file is an example. Create a .env.local file in the root directory
# and add your local development credentials there.
# DO NOT COMMIT .env.local or any other .env files containing secrets.

NEXT_PUBLIC_USE_EMULATORS=true
RUN_LISTENER_IN_DEV=true
FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"

# --- FIREBASE CLIENT SDK (for browser) ---
# These are safe to be public and are used by the client-side Firebase SDK.
# Get these from your Firebase project settings -> General tab.
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyDAzrcFxeDlHSoRqIuKvB_MCZhErXeo1Jc
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=localhost:9099
NEXT_PUBLIC_FIREBASE_PROJECT_ID=oadro-radio-dev
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
NEXT_PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef1234567890abcdef

# --- FIREBASE ADMIN SDK (for server) ---
# These are sensitive credentials and should NEVER be exposed to the client.
# Get these from your Firebase project settings -> Service accounts -> Generate new private key.
FIREBASE_PROJECT_ID=oadro-radio-dev
FIREBASE_CLIENT_EMAIL=
# IMPORTANT: The private key must be enclosed in quotes and have newlines escaped as `\n`.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# --- DISCORD INTEGRATION ---
# Your Discord server (guild) ID.
OADRO_GUILD_ID=1370875920528703629
# A comma-separated list of role IDs in your Discord server that grant VIP status.
DISCORD_VIP_ROLE_IDS=1378697499853979728
DISCORD_MODERATOR_ROLE_IDS=1378689936856715265
