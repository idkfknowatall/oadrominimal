/**
 * Simple metrics calculation for the simplified version
 */

export function calculateEngagementMetrics(): number {
  // Return a random number between 8-15 for listener count
  // In a real implementation, this would calculate actual metrics
  return Math.floor(Math.random() * 8) + 8;
}